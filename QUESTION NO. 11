#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40, 50, 60}; // Example array
    int fifthElement = arr[4]; // Accessing the 5th element (arrays are 0-indexed)

    printf("The 5th element is: %d\n", fifthElement);

    return 0;
}
